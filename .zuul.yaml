- project:
    name: bzhaoopenstack/spark
    check:
      jobs:
        - greenplum-build-arm64

- job:
    name: greenplum-build-arm64
    parent: init-test
    description: |
      The spark build in openlab cluster.
    run: .zuul/playbooks/greenplum-build/run.yaml
    nodeset: ubuntu-xenial-arm64
    timeout: 86400
